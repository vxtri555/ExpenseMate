# Copy this file to .env and paste your MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/expensemate?retryWrites=true&w=majority
PORT=5000
# Optional: only allow the frontend from this origin (leave empty for local dev)
CLIENT_ORIGIN=
